{
  "name": "chessknight-landing",
  "version": "1.0.0-alpha.1",
  "description": "Source files for Chessknight Game landing",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm:build:tsc -- --watch\" \"npm:build:sass -- --watch\" \"cd ../ && static-server\"",
    "build": "npm run build:tsc && npm run build:sass",
    "build:tsc": "tsc",
    "build:sass": "sass v3/styles/index.scss ../style.css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.1.0",
    "sass": "^1.51.0"
  }
}
