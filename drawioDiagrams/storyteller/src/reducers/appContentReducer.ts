import { IAction } from '../api/actions/IAction';
import { IViewItem } from '../api/IViewItem';
import { IAppData } from '../api/IAppData';

export const appContentReducer = function(state: IViewItem, action: IAction) {
  
}