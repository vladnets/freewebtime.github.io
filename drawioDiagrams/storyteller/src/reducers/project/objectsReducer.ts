import { IAction } from '../../api/IAction';
import { IObject } from '../../api/project/IObject';
import { IHash } from '../../api/IHash';

export const objectsReducer = (state: IHash<IObject>, action: IAction) => {
  return state;
}